CREATE TABLE Orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    size VARCHAR(20) NOT NULL,
    base VARCHAR(50) NOT NULL,
    extra_cheese VARCHAR(20) NOT NULL,s
    dip TEXT,
    bread TEXT,
    dessert TEXT,
)